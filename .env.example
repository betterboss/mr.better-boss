# Mr. Better Boss - Environment Variables
# Copy this file to .env.local and fill in your values

# JWT Secret for session tokens (generate a random string)
JWT_SECRET=your-random-secret-here-change-this-in-production

# Optional: Default Anthropic API key (users can also add their own in Settings)
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: Default JobTread API key (users can also add their own in Settings)
# JOBTREAD_API_KEY=your-jobtread-api-key
