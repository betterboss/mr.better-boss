# Mr. Better Boss - Environment Variables
# Set these in Vercel Dashboard > Settings > Environment Variables

# REQUIRED: JWT Secret for session tokens (generate a random 32+ char string)
JWT_SECRET=change-this-to-a-random-secret-string

# OPTIONAL: Default API keys (users can also add their own in the Settings panel)
# ANTHROPIC_API_KEY=sk-ant-...
# JOBTREAD_API_KEY=your-jobtread-api-key
